<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="VerticeMobile.MainPage"
             Title="Control de Productos">

    <!-- AÑADIMOS EL BOTÓN DE AJUSTES EN LA BARRA SUPERIOR -->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="⚙️ Ajustes" Clicked="OnSettingsClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30,0" VerticalOptions="Center">

            <Label Text="Buscador de Productos" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" />

            <!-- Buscador y Botón de Cámara en la misma línea -->
            <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10">
                <Entry Grid.Column="0" x:Name="txtBarcode" Placeholder="Ingrese código..." Keyboard="Numeric" ClearButtonVisibility="WhileEditing" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>
                <Button Grid.Column="1" x:Name="btnScan" Text="📷" FontSize="20" Clicked="OnToggleScannerClicked" BackgroundColor="#607D8B" WidthRequest="60" />
            </Grid>

            <Grid ColumnDefinitions="*, *" ColumnSpacing="10">
                <Button Grid.Column="0" x:Name="btnSearch" Text="Por Código" Clicked="OnSearchClicked" BackgroundColor="#1976D2" TextColor="White" />
                <Button Grid.Column="1" x:Name="btnSearchByName" Text="Por Nombre" Clicked="OnSearchByNameClicked" BackgroundColor="#9C27B0" TextColor="White" />
            </Grid>

            <!-- ¡ATENCIÓN! ELIMINAMOS EL <Grid x:Name="borderScanner"> POR COMPLETO -->
            <!-- La cámara ya no vive escondida gastando recursos en esta pantalla -->


            <!-- TARJETA 1: RESULTADO (GET) -->
            <Border x:Name="borderResult" IsVisible="False" BackgroundColor="#f8f9fa" Padding="15">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="10">
                    <Label x:Name="lblName" FontSize="20" FontAttributes="Bold" TextColor="#333" />
                    <HorizontalStackLayout Spacing="15">
                        <Label x:Name="lblCostPrice" FontSize="18" TextColor="#c62828" FontAttributes="Bold" />
                        <Label x:Name="lblSalePrice" FontSize="18" TextColor="Green" FontAttributes="Bold" />
                    </HorizontalStackLayout>
                    <Label x:Name="lblStock" FontSize="16" TextColor="#555" />

                    <Button x:Name="btnEditMode" Text="Modificar Producto" Clicked="OnEditModeClicked" BackgroundColor="#1976D2" TextColor="White" Margin="0,10,0,0"/>
                </VerticalStackLayout>
            </Border>

            <!-- TARJETA 2: CREACIÓN (POST) -->
            <Border x:Name="borderCreate" IsVisible="False" BackgroundColor="#fff3e0" Padding="15">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="15">
                    <Label Text="¡Producto no encontrado!" FontSize="16" TextColor="#e65100" FontAttributes="Bold" HorizontalOptions="Center"/>
                    <Label Text="Completá los datos para darlo de alta:" FontSize="14" HorizontalOptions="Center" />

                    <Entry x:Name="txtNewName" Placeholder="Nombre del producto" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>

                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">
                        <Entry Grid.Column="0" x:Name="txtNewCost" Placeholder="Costo $" Keyboard="Numeric" TextChanged="OnCreateCalculationChanged" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>
                        <Entry Grid.Column="1" x:Name="txtNewPercentage" Placeholder="Ganancia %" Keyboard="Numeric" TextChanged="OnCreateCalculationChanged" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>
                        <Entry Grid.Column="2" x:Name="txtNewPrice" Placeholder="Venta $" Keyboard="Numeric" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>
                    </Grid>

                    <Entry x:Name="txtNewStock" Placeholder="Stock Inicial" Keyboard="Numeric" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>

                    <Button x:Name="btnCreate" Text="Guardar Producto" Clicked="OnCreateClicked" BackgroundColor="#e65100" TextColor="White" />
                </VerticalStackLayout>
            </Border>

            <!-- TARJETA 3: EDICIÓN (PUT) -->
            <Border x:Name="borderEdit" IsVisible="False" BackgroundColor="#e3f2fd" Padding="15">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="15">
                    <Label Text="Editando Producto:" FontSize="16" TextColor="#1565c0" FontAttributes="Bold" HorizontalOptions="Center"/>

                    <Entry x:Name="txtEditName" Placeholder="Nombre del producto" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>

                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">
                        <Entry Grid.Column="0" x:Name="txtEditCost" Placeholder="Costo $" Keyboard="Numeric" TextChanged="OnEditCalculationChanged" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>
                        <Entry Grid.Column="1" x:Name="txtEditPercentage" Placeholder="Ganancia %" Keyboard="Numeric" TextChanged="OnEditCalculationChanged" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>
                        <Entry Grid.Column="2" x:Name="txtEditPrice" Placeholder="Venta $" Keyboard="Numeric" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>
                    </Grid>

                    <Entry x:Name="txtEditStock" Placeholder="Stock Actual" Keyboard="Numeric" IsTextPredictionEnabled="False" IsSpellCheckEnabled="False"/>

                    <Button x:Name="btnSaveEdit" Text="Actualizar Producto" Clicked="OnSaveEditClicked" BackgroundColor="#1565c0" TextColor="White" />
                    <Button x:Name="btnCancelEdit" Text="Cancelar" Clicked="OnCancelEditClicked" BackgroundColor="Transparent" TextColor="#1565c0" />
                </VerticalStackLayout>
            </Border>

            <Label x:Name="lblError" TextColor="Red" IsVisible="False" HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>